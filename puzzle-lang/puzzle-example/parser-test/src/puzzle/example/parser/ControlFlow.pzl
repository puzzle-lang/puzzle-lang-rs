package puzzle.example.ast

fun main() {
    val n = 500
    val a = if (true) 1 else 2
    if (123 + 2 == 3) {
        println(true)
    } else {
        println(false)
    }.printLog()
    val value1 = 1 + if (cursor.check(1)) 1 else { 2 + 3 }
    println(value1)

    val str = "Hello"
    val num = str as? Int
    val isStr = str is String
    println(3 + if (true) 1 else 2)
    val result1 = "start: " + match (value) {
        1 -> "1"
        2, 2 + 3 -> { "2 or 5" }
        4 if value.isNumber() -> { "4 is Number" }
        5 if (value.isInt()) -> {
            println("5 is Int")
            "5" + " is Number"
        }
        else -> "else"
    }
    val result2 = match {
        value2 is String && value2 == "1" -> {
            "1 is String"
        }

        value2 == "2" -> "2"

        else -> {
            println("else")
            "else"
        }
    }
    val result3 = match ("Hello") {
        is String as name -> name
        else -> ""
    }

    var count = 0
    while (count < 100) {
        count++
    }

    do {
        ++count
        if (count == 150) {
            break
        } else if (count == 140) {
            count++
            continue
        }
    } while (count < 200)

    val [n1, n2] = loop {
        val x = readInt()
        if (x >= 0) {
            break [1, 2 + 3]
        }
        println("negative, try again")
    }

    loop1@ loop {
        val num = randomInt()
        if (num % 2 == 0) {
            break@loop1 num
        }
    }

    loop if (randomInt() % 100 == 0) break

    val items = listOf("1", "3", "5")
    name1@ for (item in items) {
        println(item)
        if (item == "1") break@name1
    }
    for ([index, item] in items) {
        println("index: " + index + ", item: " + item)
    }
    for (item in items) println(item)

    var i = 0
    while (i < 10) i++

    do i++ while (i < 10)

    val lambda = lambda1@ {
        return@lambda1
    }

    val [name, age, height] = {
        ["H", 18, 178]
    }
}

fun add(num1: Int, num2: Int): Int {
    if (num1 == num2) return num1 * 2
    return@add num1 + num2
}